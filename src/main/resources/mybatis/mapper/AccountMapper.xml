<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.atm.mybatis.AccountMapper">

    <select id="findByAccountNumber" parameterType="int" resultType="com.atm.model.Account">
        SELECT account_number AS accountNumber, pin, balance
        FROM accounts
        WHERE account_number = #{accountNumber}
    </select>

    <update id="updateBalance" parameterType="com.atm.model.Account">
        UPDATE accounts
        SET balance = #{balance}
        WHERE account_number = #{accountNumber}
    </update>

</mapper>
